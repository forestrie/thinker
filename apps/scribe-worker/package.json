{
	"name": "scribe-worker",
	"version": "0.0.1",
	"private": true,
	"type": "module",
	"description": "Worker hosting the Scribe Think Durable Object (one instance per user)",
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy",
		"typecheck": "tsc -p tsconfig.json",
		"test": "vitest run",
		"build": "wrangler deploy --dry-run --outdir dist"
	},
	"dependencies": {
		"@forestrie/think-scribe": "workspace:*",
		"@noble/curves": "^2.3.0",
		"@noble/hashes": "^2.3.0",
		"agents": "^0.20.1"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^4.20250801.0",
		"typescript": "^5.7.0",
		"wrangler": "^4.0.0",
		"vitest": "^3.2.4"
	}
}
