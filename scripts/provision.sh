#!/usr/bin/env bash
# provision.sh — stand up (or reuse) a univocity instance + grant via the
# ietf-126-demo `forestrie` CLI, and emit (chainId, contract, R, genesis) into
# config/instance.jsonc. Univocity registration/setup is a mandate concern —
# no mandate fork; this drives the existing demo tooling (plan §11 O6).
#
# M2 work item — not implemented in the M0 scaffold.
set -euo pipefail
echo "provision.sh: not implemented until M2 (see plan §9)." >&2
exit 1
